# ClassicMac 2.2.0 beta 3

ClassicMac 2.2.0 beta 3 is a GXMetal corrective prerelease for manual game
testing. It restores Quake III world geometry that regressed in the earlier
2.2.0 betas and turns the failure into a permanent visual regression gate.

The native Cocoa machine window remains the default. The private browser/VNC
viewer introduced in beta 1 remains available as an optional per-machine
setting.

## Quake III pointer-fan correction

- ATI OpenGL private slot 60 submits pointer-based triangle fans. Its fourth
  argument describes whether the ATI GLD clipped the staged fan; zero is a
  valid ordinary fan, not an instruction to discard the draw.
- GXMetal now renders both zero-marker and nonzero-marker slot-60 fans when
  their bounded vertex count is valid. Invalid counts remain rejected.
- This restores Quake III's missing floors, walls, pedestals, arches, and
  other polygon-shaped world surfaces without changing the protocol or
  relaxing GXMetal's pointer and count validation.
- A silent Q3DM1 route captures six reviewed gameplay viewpoints: the spawn
  courtyard, large carved arch, small side arch, under-arch angle, lit
  passage, and side approach. A region-scoped near-black-pixel oracle catches
  the former holes while excluding the sky, HUD, and animated content.

## Qualification tooling and current status

- The sweep harness can now apply inclusive RGB-range assertions to a stable
  subregion, making visual regression checks more specific than whole-frame
  color counts.
- The checked-in compatibility ledger now records Cro-Mag Rally's accelerated
  Practice/Desert race and steering pass, two current-driver Combat Mission
  Setup-to-Orders rendering passes, and Unreal Tournament's sound-disabled
  current-driver menu gate.
- Dark Vengeance and HAVOC remain pre-3D application/media blockers. Myth II
  remains a confirmed GXMetal transition blocker after its 43 intentional
  blank texture preallocations. Those results are documented, not hidden by
  this focused Quake correction.

This remains a prerelease. Bugdom and Future Cop retain representative
gameplay evidence, while several expansion titles still need some combination
of longer gameplay, input, clean quit/relaunch, and audio coverage. See the
[current ten-game compatibility ledger](https://github.com/amcchord/ClassicMac/blob/v2.2.0-beta.3/docs/gxmetal-ten-game-compatibility.md)
and
[scripted sweep guide](https://github.com/amcchord/ClassicMac/blob/v2.2.0-beta.3/docs/gxmetal-game-sweep.md)
for the evidence-backed matrix and reproduction procedures.

## Validation

The prerelease is gated by the complete GXMetal native protocol, queue, guest
transport, renderer, dirty tracking, pipeline, ATI compatibility, RAVE
compatibility, AGL probe, Metal render/readback, VNC harness, diagnostics, and
PEF suites; the Swift app suite; warning-as-error PowerPC guest builds; a clean
QEMU rebuild and transport self-checks; signed-bundle verification; Apple
notarization and stapling; mounted-DMG verification; and Gatekeeper assessment.
